---
import { cn } from '../utils/cn';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span class={cn('rounded-full bg-bg-secondary px-2 py-0.5 text-xs text-text-secondary', className)}>
  <slot />
</span>
